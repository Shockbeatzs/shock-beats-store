# Beats Frontend (React)
## Quick start
1. Install dependencies:
   ```bash
   cd frontend
   npm install
   ```
2. Start dev server:
   ```bash
   REACT_APP_API=http://localhost:4000 npm start
   ```
This is a minimal React app with login/register, upload and list features.
